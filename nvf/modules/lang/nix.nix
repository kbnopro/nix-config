{
  ...
}: {
  config.vim.languages.nix = {
    enable = true;
    format = {
      enable = true;
    };
    lsp = {
      enable = true;
    };
    treesitter = {
      enable = true;
    };
  };
}
